# Zerde 109 — План 2: API (Fastify на витрине)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Поднять `apps/api` — Fastify-сервис поверх витрины `appeals` из Плана 1 — со всеми эндпоинтами трёх модулей (ситуационный центр, смарт-приём, ассистент оператора), типизированными сквозь `@zerde/types`, с валидацией `zod` и единым конвертом ошибок.

**Architecture:** Новый воркспейс `apps/api`. Слой доступа к данным изолирован в `src/repo/*` (чистые функции `(pool, params) => rows`), поверх — тонкие роут-плагины по модулям. Свой `pg.Pool` (не синглтон ингеста). Переиспользуем из `@zerde/ingest` чистую логику: `detectLanguage`, `classifyWith`, `THEME_RULES`, новый `classifyText`. Ответы — типы из `@zerde/types`. Read-only относительно исходных данных; правки оператора идут в `mutations`.

**Tech Stack:** Fastify 5, `@fastify/cors`, `@fastify/sensible`, `zod` (+ `fastify-type-provider-zod`), `pino` (встроен в Fastify), `pg`, `exceljs` (xlsx), `pdfkit` (pdf), `tsx`, Vitest + `fastify.inject`.

## Global Constraints

- **Node:** 22.x. **Package manager:** npm workspaces. **Модули:** ESM во всех пакетах.
- **TypeScript:** strict, `noUncheckedIndexedAccess`. `npm run check` = `tsc --noEmit` по `packages/types`, `packages/ingest`, `apps/api` — должен проходить.
- **PostgreSQL:** БД `zerde109` (рабочая), `zerde109_test` (тесты). Подключение через `DATABASE_URL` / `TEST_DATABASE_URL`. Postgres на trust-auth (локально).
- **SQL:** только параметризованные запросы. NL-запрос собирает SQL из фиксированного билдера, пользовательский текст в строку запроса не попадает никогда.
- **Порты:** API — `3001`. Дев-хост — `127.0.0.1`.
- **Префикс маршрутов:** все — под `/api`.
- **Конверт ошибки:** `{ "error": { "code": string, "message": string } }`. Валидация → 400 `code:"bad_request"`; не найдено → 404 `code:"not_found"`; NL не распознан → 422 `code:"unrecognized_query"`; прочее → 500 `code:"internal"` + лог.
- **Формат дат в ответах:** ISO 8601 (UTC, `Z`). Диапазоны фильтров — `from`/`to` в `YYYY-MM-DD` (включительно по дню).
- **Пагинация:** `page` (1-based, дефолт 1), `pageSize` (дефолт 50, максимум 200). Ответ — `{ items, page, pageSize, total }`.
- **Коммиты:** после каждой задачи. Тело сообщения — по необходимости. В конце каждого:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  ```
- **Таксономия тем** и **единый ЖЦ статуса** — как в Плане 1 (17 тем, 5 статусов).

## Что уже есть (из Плана 1) — не переписывать

- **БД `zerde109`** заполнена: `appeals` (~990k строк, колонки см. `db/migrations/0001_init.sql`), `regions`/`themes`/`services`/`theme_map` (засеяны), `templates` (пустая), `mutations` (пустая), матвьюхи `mv_daily_counts`/`mv_theme_totals`/`mv_region_totals`/`mv_operator_load`, таблицы `spikes`/`forecasts`.
- **`@zerde/types`** экспортирует: `ThemeCode`, `THEME_CODES`, `AppealStatus`, `AppealType`, `Channel`, `Language`, `Priority`, `Appeal`, `RegionRef`, `ThemeRef`, `ServiceRef`.
- **`@zerde/ingest`** (`src/…`): `themeMap.ts` → `classifyWith(rules, inputs)`, `loadThemeMap(pool)`, типы `ThemeRule`/`ThemeClassifier`; `themeRules.ts` → `THEME_RULES`; `normalize.ts` → `detectLanguage`, `parseDateTime`, `priorityHeuristic`, `buildSearchText`, …; `taxonomy.ts` → `THEME_SERVICE`; `db.ts` → `getPool`/`q`; `paths.ts` → `isMain`/`REPO_ROOT`; `analytics/*`.
- **`db/migrations/`** — нумерованные `.sql`, накат раннером `packages/ingest/src/migrate.ts` (`runMigrations`). **`db/seeds/`** — `01..04`, раннер `runSeeds`.
- **Тесты** гоняются последовательно (`vitest fileParallelism:false`), делят `zerde109_test`, пересоздают схему в `beforeAll`.

---

## Целевая структура файлов (создаётся в этом плане)

```
packages/ingest/
  src/lib.ts                       barrel: re-export themeMap + normalize + THEME_RULES + classifyText
  src/classifyText.ts              classifyText(text, ctx) -> ClassifyResult (чистая)
  src/eval.ts                      скрипт `npm run eval` — бэктест classifyText -> таблицы model_eval*
  src/analytics/modelEval.ts       computeModelEval(pool) — расчёт метрик
  package.json                     + "exports" map, + "eval" script
db/
  migrations/0004_model_eval.sql   model_eval_overview, model_eval_themes, model_eval_confusion
  seeds/05_templates.sql           ~16 типовых ответов (RU/KK) по темам

apps/api/
  package.json  tsconfig.json  vitest.config.ts
  src/
    server.ts                      buildServer(): FastifyInstance (плагины, error handler, routes)
    start.ts                       точка входа: buildServer().listen(3001)
    db.ts                          локальный pg.Pool + q<T>()
    env.ts                         чтение/проверка env (DATABASE_URL, PORT)
    errors.ts                      AppError + toEnvelope + setErrorHandler
    schema.ts                      общие zod-схемы фильтров (region, from, to, theme, page…)
    repo/
      meta.ts kpi.ts timeseries.ts breakdown.ts appeals.ts spikes.ts
      forecast.ts similar.ts templates.ts mutations.ts nlQuery.ts modelEval.ts report.ts
    routes/
      meta.ts commandCenter.ts intake.ts operator.ts report.ts
    nl/
      intents.ts                   5 интент-парсеров -> {metric, dims, filters}
      sqlBuilder.ts                {metric,dims,filters} -> {sql, params} (фиксированные шаблоны)
  tests/
    meta.test.ts kpi.test.ts timeseries.test.ts breakdown.test.ts
    appeals.test.ts spikes-forecast.test.ts classify.test.ts model-eval.test.ts
    nl-query.test.ts operator.test.ts report.test.ts
    _server.ts                     helper: buildServer + seeded test DB + fixture appeals
packages/types/src/index.ts        + API-типы (см. Task 2)
```

Слой `repo/*` — единственное место с SQL. Роут делает: zod-парс query/body → вызов repo → отдать (тип из `@zerde/types`). Переезд на другой источник = замена `repo/*`.

---

## Task 1: Экспорт-баррель `@zerde/ingest` + `apps/api` скелет

**Files:**
- Create: `packages/ingest/src/lib.ts`
- Modify: `packages/ingest/package.json` (добавить `exports`)
- Create: `apps/api/package.json`, `apps/api/tsconfig.json`, `apps/api/vitest.config.ts`
- Create: `apps/api/src/env.ts`, `apps/api/src/db.ts`, `apps/api/src/errors.ts`, `apps/api/src/server.ts`, `apps/api/src/start.ts`
- Modify: корневой `package.json` (scripts: `dev:api`, обновить `check`), `eslint.config.js` (убрать `apps/**` из ignores)
- Test: `apps/api/tests/health.test.ts`

**Interfaces:**
- Produces:
  - `@zerde/ingest` barrel (`import { classifyWith, detectLanguage, THEME_RULES } from "@zerde/ingest"`).
  - `apps/api/src/db.ts`: `getPool(): pg.Pool` (свой синглтон из `DATABASE_URL`), `q<T>(pool, sql, params?): Promise<T[]>`.
  - `apps/api/src/errors.ts`: `class AppError extends Error { code: string; statusCode: number }`, `notFound(msg)`, `badRequest(msg)`, `unrecognized(msg)`; `registerErrorHandler(app)`.
  - `apps/api/src/server.ts`: `buildServer(opts?: { logger?: boolean }): Promise<FastifyInstance>` — регистрирует `@fastify/cors` (origin `true` в dev), `@fastify/sensible`, error handler, health route `GET /api/health` → `{ ok: true }`. Роут-плагины подключаются здесь (по мере добавления в следующих задачах).

- [ ] **Step 1: `packages/ingest/src/lib.ts`**

```ts
export * from "./themeMap";
export * from "./normalize";
export { THEME_RULES, type ThemeRule } from "./themeRules";
export { THEME_SERVICE } from "./taxonomy";
export { classifyText, type ClassifyTextResult } from "./classifyText";
```

(`classifyText` появится в Task 9 — до тех пор строку с ним закомментировать; раскомментировать в Task 9, Step 1.)

- [ ] **Step 2: `packages/ingest/package.json` — добавить `exports` и не сломать существующее**

В корень объекта добавить (это ЕДИНСТВЕННЫЙ способ, которым `apps/api` и его тесты тянут код из `@zerde/ingest` — держаться его везде):
```json
"main": "./src/lib.ts",
"exports": {
  ".": "./src/lib.ts",
  "./migrate": "./src/migrate.ts",
  "./seed": "./src/seed.ts",
  "./db": "./src/db.ts",
  "./analytics/views": "./src/analytics/views.ts",
  "./analytics/spikes": "./src/analytics/spikes.ts",
  "./analytics/forecasts": "./src/analytics/forecasts.ts",
  "./loadEnv": "./src/loadEnv.ts",
  "./package.json": "./package.json"
}
```
Существующие `scripts` не трогать. Импорты вида `@zerde/ingest/migrate` резолвятся через этот map; относительными путями `../../../packages/ingest/...` больше НЕ пользуемся.

- [ ] **Step 3: `apps/api/package.json`**

```json
{
  "name": "@zerde/api",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/start.ts",
    "start": "tsx src/start.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/sensible": "^6.0.1",
    "@zerde/ingest": "*",
    "@zerde/types": "*",
    "exceljs": "^4.4.0",
    "fastify": "^5.1.0",
    "pdfkit": "^0.15.1",
    "pg": "^8.13.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/pdfkit": "^0.13.5",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 4: `apps/api/tsconfig.json` и `vitest.config.ts`**

`tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "types": ["node"], "noEmit": true },
  "include": ["src", "tests"]
}
```

`vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: {
    include: ["tests/**/*.test.ts"],
    setupFiles: ["@zerde/ingest/loadEnv"],
    hookTimeout: 30_000,
    fileParallelism: false,
  },
});
```

- [ ] **Step 5: `apps/api/src/env.ts`**

```ts
export interface ApiEnv {
  databaseUrl: string;
  port: number;
}

export function readEnv(): ApiEnv {
  const databaseUrl = process.env.DATABASE_URL;
  if (!databaseUrl) throw new Error("DATABASE_URL is not set");
  return { databaseUrl, port: Number(process.env.PORT ?? 3001) };
}
```

- [ ] **Step 6: `apps/api/src/db.ts`**

```ts
import pg from "pg";
import { readEnv } from "./env";

let pool: pg.Pool | null = null;

export function getPool(): pg.Pool {
  if (!pool) pool = new pg.Pool({ connectionString: readEnv().databaseUrl, max: 12 });
  return pool;
}

export async function q<T extends pg.QueryResultRow = pg.QueryResultRow>(
  pool: pg.Pool,
  sql: string,
  params?: unknown[],
): Promise<T[]> {
  const res = await pool.query<T>(sql, params as unknown[] | undefined);
  return res.rows;
}
```

- [ ] **Step 7: `apps/api/src/errors.ts`**

```ts
import type { FastifyInstance } from "fastify";

export class AppError extends Error {
  constructor(
    readonly code: string,
    message: string,
    readonly statusCode: number,
  ) {
    super(message);
  }
}

export const badRequest = (m: string): AppError => new AppError("bad_request", m, 400);
export const notFound = (m: string): AppError => new AppError("not_found", m, 404);
export const unrecognized = (m: string): AppError => new AppError("unrecognized_query", m, 422);

export function registerErrorHandler(app: FastifyInstance): void {
  app.setErrorHandler((err, _req, reply) => {
    if (err instanceof AppError) {
      reply.code(err.statusCode).send({ error: { code: err.code, message: err.message } });
      return;
    }
    if ((err as { validation?: unknown }).validation) {
      reply.code(400).send({ error: { code: "bad_request", message: err.message } });
      return;
    }
    app.log.error(err);
    reply.code(500).send({ error: { code: "internal", message: "internal error" } });
  });
  app.setNotFoundHandler((_req, reply) => {
    reply.code(404).send({ error: { code: "not_found", message: "route not found" } });
  });
}
```

- [ ] **Step 8: `apps/api/src/server.ts`**

```ts
import Fastify, { type FastifyInstance } from "fastify";
import cors from "@fastify/cors";
import sensible from "@fastify/sensible";
import { registerErrorHandler } from "./errors";

export async function buildServer(opts: { logger?: boolean } = {}): Promise<FastifyInstance> {
  const app = Fastify({ logger: opts.logger ?? false });
  await app.register(cors, { origin: true });
  await app.register(sensible);
  registerErrorHandler(app);

  app.get("/api/health", async () => ({ ok: true }));

  // Роут-плагины регистрируются здесь по мере добавления:
  // await app.register(metaRoutes);            // Task 3
  // await app.register(commandCenterRoutes);   // Tasks 4-8, 11
  // await app.register(intakeRoutes);          // Tasks 9-10
  // await app.register(operatorRoutes);        // Task 12
  // await app.register(reportRoutes);          // Task 13

  return app;
}
```

- [ ] **Step 9: `apps/api/src/start.ts`**

```ts
import "@zerde/ingest/loadEnv";
import { buildServer } from "./server";
import { readEnv } from "./env";

const { port } = readEnv();
const app = await buildServer({ logger: true });
await app.listen({ host: "127.0.0.1", port });
```

- [ ] **Step 10: Корневой `package.json` и `eslint.config.js`**

`package.json` scripts:
```json
"check": "tsc -p packages/types --noEmit && tsc -p packages/ingest --noEmit && tsc -p apps/api --noEmit",
"dev:api": "npm run dev -w @zerde/api",
"test": "npm test --workspaces --if-present"
```

`eslint.config.js` — из `ignores` убрать `"apps/**"`.

- [ ] **Step 11: `apps/api/tests/health.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import { buildServer } from "../src/server";

let app: FastifyInstance;
beforeAll(async () => { app = await buildServer(); });
afterAll(async () => { await app.close(); });

test("GET /api/health -> { ok: true }", async () => {
  const res = await app.inject({ method: "GET", url: "/api/health" });
  expect(res.statusCode).toBe(200);
  expect(res.json()).toEqual({ ok: true });
});

test("unknown route -> 404 envelope", async () => {
  const res = await app.inject({ method: "GET", url: "/api/nope" });
  expect(res.statusCode).toBe(404);
  expect(res.json()).toEqual({ error: { code: "not_found", message: "route not found" } });
});
```

- [ ] **Step 12: Установить и проверить**

Run:
```bash
npm install
npm run check
npm test -w @zerde/api
```
Expected: install ок; `check` exit 0; health-тесты PASS (2).

- [ ] **Step 13: Commit**

```bash
git add -A
git commit -m "feat(api): scaffold Fastify app (@zerde/api) + @zerde/ingest barrel export

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 2: API-типы в `@zerde/types`

**Files:**
- Modify: `packages/types/src/index.ts`
- Test: `packages/types` компилируется (`tsc`), плюс типы используются в тестах следующих задач.

**Interfaces:**
- Produces (добавить в `index.ts`, ничего не удаляя):

```ts
// --- фильтры ---
export interface RangeFilter { region?: string; theme?: ThemeCode; from?: string; to?: string }
export type Granularity = "day" | "week" | "month";
export type BreakdownDim = "region" | "theme" | "status" | "channel";

// --- ситуационный центр ---
export interface Kpi {
  total: number;
  prevTotal: number;
  deltaPct: number;
  overdueShare: number;      // 0..1
  avgCloseHours: number | null;
  repeatShare: number;       // 0..1
  openNow: number;
}
export interface TimePoint { bucket: string; count: number; overdue: number }
export interface TimeseriesResponse { granularity: Granularity; points: TimePoint[] }
export interface BreakdownRow { key: string; label: string; count: number; overdue: number; deltaPct: number }
export interface SpikeRow {
  region: string; theme: ThemeCode; day: string;
  baseline: number; current: number; ratio: number; zscore: number;
  severity: "low" | "medium" | "high";
}
export interface ForecastRow { month: string; yhat: number; yhatLower: number; yhatUpper: number }
export interface ForecastResponse {
  region: string; theme: ThemeCode; method: string;
  history: { month: string; count: number }[];
  forecast: ForecastRow[];
}
export interface NlQueryResult {
  intent: string;
  sql: string;
  value: number | null;
  rows: Record<string, string | number>[];
  chart: { type: "bar" | "line"; x: string; y: string } | null;
  summary: string;
}

// --- смарт-приём ---
export interface AppealListItem {
  id: string; sourceId: string; region: string; createdAt: string;
  theme: ThemeCode; status: AppealStatus; priority: Priority;
  channel: Channel | null; preview: string; isOverdue: boolean;
}
export interface Paginated<T> { items: T[]; page: number; pageSize: number; total: number }
export interface ClassifyResult {
  theme: ThemeCode; service: string; priority: Priority;
  language: Language; confidence: number;
  entities: { address: string | null; object: string | null; problem: string | null };
}
export interface ModelEvalTheme {
  theme: ThemeCode; precision: number; recall: number; f1: number; support: number;
}
export interface ModelEval {
  method: string; computedAt: string; nHoldout: number;
  accuracy: number; macroF1: number;
  perTheme: ModelEvalTheme[];
  confusion: { actual: ThemeCode; predicted: ThemeCode; n: number }[];
}

// --- ассистент оператора ---
export interface SimilarAppeal {
  id: string; createdAt: string; region: string; theme: ThemeCode;
  preview: string; serviceOrg: string | null; resolution: string | null;
  daysToClose: number | null; similarity: number;
}
export interface DuplicateInfo {
  nearDuplicates: SimilarAppeal[];
  repeats: SimilarAppeal[];       // тот же адрес, закрыто < 30 дн назад
}
export interface Template { id: number; themeCode: ThemeCode | null; serviceCode: string | null; lang: Language; title: string; body: string }

// --- meta ---
export interface Meta {
  regions: RegionRef[];
  themes: ThemeRef[];
  services: ServiceRef[];
  channels: { code: Channel; labelRu: string; labelKk: string }[];
  statuses: { code: AppealStatus; labelRu: string; labelKk: string }[];
}
```

- [ ] **Step 1: Дописать типы в `packages/types/src/index.ts`** (блок выше, целиком).

- [ ] **Step 2: Проверить**

Run: `npm run check`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat(types): API response/request types for all three modules

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 3: `GET /api/meta` + общие zod-схемы фильтров + тест-хелпер

**Files:**
- Create: `apps/api/src/schema.ts`, `apps/api/src/repo/meta.ts`, `apps/api/src/routes/meta.ts`
- Create: `apps/api/tests/_server.ts`
- Modify: `apps/api/src/server.ts` (register `metaRoutes`)
- Test: `apps/api/tests/meta.test.ts`

**Interfaces:**
- Consumes: `getPool`/`q` (T1), типы `Meta` и др. (T2).
- Produces:
  - `schema.ts`: `zRange` (zod-объект: `region?: string`, `theme?: enum(THEME_CODES)`, `from?: /^\d{4}-\d{2}-\d{2}$/`, `to?: same`), `zPagination` (`page` coerce int ≥1 дефолт 1, `pageSize` coerce int 1..200 дефолт 50), `parseRange(q): RangeFilter`.
  - `repo/meta.ts`: `getMeta(pool): Promise<Meta>` — 4 SELECT (regions, themes, services) + статические словари каналов/статусов (RU/KK подписи прямо в коде).
  - `routes/meta.ts`: Fastify-плагин, `GET /api/meta` → `Meta`.
  - `tests/_server.ts`: `makeTestServer(): Promise<{ app, pool }>` — поднимает БД `zerde109_test`, `runMigrations` + `runSeeds` (из `@zerde/ingest`), грузит фикстуру ~30 обращений (функция `seedAppeals(pool)` — детерминированный набор со всеми 7 регионами, разными темами/статусами/датами за 2024–2025, парой просрочек и повторов), `refreshViews`, затем `buildServer()`. Экспортирует и `seedAppeals` отдельно.

- [ ] **Step 1: `apps/api/src/schema.ts`**

```ts
import { z } from "zod";
import { THEME_CODES } from "@zerde/types";
import type { RangeFilter } from "@zerde/types";

const dateStr = z.string().regex(/^\d{4}-\d{2}-\d{2}$/);

export const zRange = z.object({
  region: z.string().min(1).optional(),
  theme: z.enum(THEME_CODES as [string, ...string[]]).optional(),
  from: dateStr.optional(),
  to: dateStr.optional(),
});

export const zPagination = z.object({
  page: z.coerce.number().int().min(1).default(1),
  pageSize: z.coerce.number().int().min(1).max(200).default(50),
});

export function parseRange(q: unknown): RangeFilter {
  return zRange.parse(q) as RangeFilter;
}

/** SQL-фрагмент `WHERE` + параметры из RangeFilter (для repo). */
export function rangeWhere(r: RangeFilter, startIdx = 1): { clause: string; params: unknown[] } {
  const parts: string[] = [];
  const params: unknown[] = [];
  let i = startIdx;
  if (r.region) { parts.push(`region = $${i++}`); params.push(r.region); }
  if (r.theme) { parts.push(`theme = $${i++}`); params.push(r.theme); }
  if (r.from) { parts.push(`created_at >= $${i++}`); params.push(`${r.from}T00:00:00+05:00`); }
  if (r.to) { parts.push(`created_at < ($${i++}::date + interval '1 day')`); params.push(r.to); }
  return { clause: parts.length ? `WHERE ${parts.join(" AND ")}` : "", params };
}
```

- [ ] **Step 2: `apps/api/src/repo/meta.ts`**

```ts
import type pg from "pg";
import type { Meta } from "@zerde/types";
import { q } from "../db";

const CHANNELS: Meta["channels"] = [
  { code: "ekc109", labelRu: "ЕКЦ 109", labelKk: "ЕБО 109" },
  { code: "whatsapp", labelRu: "WhatsApp", labelKk: "WhatsApp" },
  { code: "instagram", labelRu: "Instagram", labelKk: "Instagram" },
  { code: "telegram", labelRu: "Telegram", labelKk: "Telegram" },
  { code: "facebook", labelRu: "Facebook", labelKk: "Facebook" },
  { code: "mobile", labelRu: "Мобильное приложение", labelKk: "Мобильді қосымша" },
  { code: "web", labelRu: "Портал", labelKk: "Портал" },
  { code: "social", labelRu: "Соцсети", labelKk: "Әлеуметтік желілер" },
  { code: "monitoring", labelRu: "Мониторинг", labelKk: "Мониторинг" },
  { code: "other", labelRu: "Другое", labelKk: "Басқа" },
];
const STATUSES: Meta["statuses"] = [
  { code: "new", labelRu: "Новое", labelKk: "Жаңа" },
  { code: "routed", labelRu: "Передано в службу", labelKk: "Қызметке берілді" },
  { code: "in_progress", labelRu: "В работе", labelKk: "Жұмыста" },
  { code: "done", labelRu: "Выполнено", labelKk: "Орындалды" },
  { code: "cancelled", labelRu: "Отменено", labelKk: "Бас тартылды" },
];

export async function getMeta(pool: pg.Pool): Promise<Meta> {
  const regions = await q<{ code: string; name_ru: string; name_kk: string; is_active: boolean }>(
    pool, "SELECT code, name_ru, name_kk, is_active FROM regions ORDER BY is_active DESC, name_ru",
  );
  const themes = await q<{ code: string; name_ru: string; name_kk: string; color: string; sort: number }>(
    pool, "SELECT code, name_ru, name_kk, color, sort FROM themes ORDER BY sort",
  );
  const services = await q<{ code: string; name_ru: string; name_kk: string }>(
    pool, "SELECT code, name_ru, name_kk FROM services ORDER BY name_ru",
  );
  return {
    regions: regions.map((r) => ({ code: r.code, nameRu: r.name_ru, nameKk: r.name_kk, isActive: r.is_active })),
    themes: themes.map((t) => ({ code: t.code as never, nameRu: t.name_ru, nameKk: t.name_kk, color: t.color, sort: t.sort })),
    services: services.map((s) => ({ code: s.code, nameRu: s.name_ru, nameKk: s.name_kk })),
    channels: CHANNELS,
    statuses: STATUSES,
  } satisfies Meta;
}
```

- [ ] **Step 3: `apps/api/src/routes/meta.ts`**

```ts
import type { FastifyInstance } from "fastify";
import { getPool } from "../db";
import { getMeta } from "../repo/meta";

export async function metaRoutes(app: FastifyInstance): Promise<void> {
  app.get("/api/meta", async () => getMeta(getPool()));
}
```

- [ ] **Step 4: Зарегистрировать в `server.ts`** — добавить `await app.register(metaRoutes);` и импорт.

- [ ] **Step 5: `apps/api/tests/_server.ts`**

```ts
import pg from "pg";
import type { FastifyInstance } from "fastify";
import { runMigrations } from "@zerde/ingest/migrate";
import { runSeeds } from "@zerde/ingest/seed";
import { refreshViews } from "@zerde/ingest/analytics/views";
import { computeSpikes } from "@zerde/ingest/analytics/spikes";
import { computeForecasts } from "@zerde/ingest/analytics/forecasts";
import { buildServer } from "../src/server";

const url =
  process.env.TEST_DATABASE_URL ?? "postgres://postgres@localhost:5432/zerde109_test";

/** ~34 детерминированных обращения: 7 регионов, ≥8 тем, статусы, 2024-01..2025-06,
 *  3 просрочки, 2 повтора с одного адреса, 4 канала, 2 языка. */
export async function seedAppeals(pool: pg.Pool): Promise<void> {
  const rows: unknown[][] = [];
  // … сгенерировать программно (цикл по регионам × темам), см. Step 6 ниже
  // помещается в один INSERT; функция реализуется в Step 6.
}

export async function makeTestServer(): Promise<{ app: FastifyInstance; pool: pg.Pool }> {
  process.env.DATABASE_URL = url;
  const pool = new pg.Pool({ connectionString: url });
  await pool.query("DROP SCHEMA public CASCADE; CREATE SCHEMA public;");
  await runMigrations(pool);
  await runSeeds(pool);
  await seedAppeals(pool);
  await refreshViews(pool);
  await computeSpikes(pool);
  await computeForecasts(pool);
  const app = await buildServer();
  return { app, pool };
}
```

Импорты внутренних модулей `@zerde/ingest` — только через подпути из `exports`-map (`@zerde/ingest/migrate`, `/seed`, `/analytics/views` и т.д.), заданные в Task 1 Step 2. Относительными путями не пользуемся.

- [ ] **Step 6: Реализовать `seedAppeals`** — вставляет фиксированный набор. Полный код:

```ts
import { THEME_CODES } from "@zerde/types";

const REGIONS = ["akmola", "almaty", "east-kazakhstan", "karaganda", "kostanay", "turkestan", "pavlodar"];
const APPEAL_COLS = [
  "id", "source_id", "region", "district", "locality", "address", "lat", "lon",
  "created_at", "closed_at", "deadline_at", "theme", "raw_category", "subcategory",
  "service_org", "status", "raw_status", "appeal_type", "channel", "language",
  "priority", "is_overdue", "sla_days", "grade", "operator", "resolution", "search_text",
];

export async function seedAppeals(pool: pg.Pool): Promise<void> {
  const themes = THEME_CODES.slice(0, 10);
  const rows: unknown[][] = [];
  let n = 0;
  for (let ri = 0; ri < REGIONS.length; ri++) {
    for (let ti = 0; ti < 5; ti++) {
      n++;
      const region = REGIONS[ri]!;
      const theme = themes[(ri + ti) % themes.length]!;
      const month = 1 + ((ri + ti) % 12);
      const created = `2024-${String(month).padStart(2, "0")}-1${ti} 09:00:00+05:00`;
      const done = ti % 2 === 0;
      const overdue = ti === 1;
      const addr = ti === 4 ? "ул. Повторная 1" : `ул. Тестовая ${n}`;
      rows.push([
        `${region}:t${n}`, `t${n}`, region, null, "Тестгород", addr, null, null,
        created, done ? `2024-${String(month).padStart(2, "0")}-2${ti} 10:00:00+05:00` : null,
        `2024-${String(month).padStart(2, "0")}-15 00:00:00+05:00`,
        theme, "тест-категория", "тест-подкатегория", ti === 0 ? "ТОО Тест" : null,
        done ? "done" : ti === 3 ? "in_progress" : "routed", null,
        ti % 3 === 0 ? "incident" : "consultation",
        ["ekc109", "whatsapp", "mobile", "web"][ti % 4], ti % 5 === 0 ? "kk" : "ru",
        overdue ? "high" : "low", overdue, 5, null, ti === 3 ? "op1" : null,
        done ? "Проблема устранена" : null,
        `тест ${theme} ${addr}`,
      ]);
      // повтор: тот же адрес, закрыт недавно
      if (ti === 4) {
        n++;
        rows.push([
          `${region}:t${n}`, `t${n}`, region, null, "Тестгород", addr, null, null,
          `2025-05-0${ri + 1} 09:00:00+05:00`, `2025-05-1${ri + 1} 09:00:00+05:00`, null,
          theme, "тест-категория", "тест-подкатегория", null,
          "done", null, "complaint", "ekc109", "ru", "medium", false, 7, null, null,
          "Вопрос решен консультацией", `повтор ${theme} ${addr}`,
        ]);
      }
    }
  }
  const per = APPEAL_COLS.length;
  const tuples = rows.map(
    (_, i) => `(${APPEAL_COLS.map((__, j) => `$${i * per + j + 1}`).join(",")})`,
  );
  await pool.query(
    `INSERT INTO appeals (${APPEAL_COLS.join(",")}) VALUES ${tuples.join(",")}`,
    rows.flat(),
  );
}
```

- [ ] **Step 7: `apps/api/tests/meta.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import pg from "pg";
import { makeTestServer } from "./_server";

let app: FastifyInstance;
let pool: pg.Pool;
beforeAll(async () => { ({ app, pool } = await makeTestServer()); });
afterAll(async () => { await app.close(); await pool.end(); });

test("GET /api/meta returns 20 regions (7 active), 17 themes, 16 services, channels, statuses", async () => {
  const res = await app.inject({ method: "GET", url: "/api/meta" });
  expect(res.statusCode).toBe(200);
  const body = res.json();
  expect(body.regions).toHaveLength(20);
  expect(body.regions.filter((r: { isActive: boolean }) => r.isActive)).toHaveLength(7);
  expect(body.themes).toHaveLength(17);
  expect(body.services).toHaveLength(16);
  expect(body.channels.length).toBeGreaterThanOrEqual(9);
  expect(body.statuses.map((s: { code: string }) => s.code)).toContain("in_progress");
});
```

- [ ] **Step 8: Прогнать**

Run: `npm run check && npm test -w @zerde/api -- meta`
Expected: `check` 0; meta-тест PASS.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat(api): GET /api/meta + filter schemas + seeded test-server helper

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Task 4: `GET /api/kpi`

**Files:**
- Create: `apps/api/src/repo/kpi.ts`, `apps/api/src/routes/commandCenter.ts`
- Modify: `apps/api/src/server.ts` (register `commandCenterRoutes`)
- Test: `apps/api/tests/kpi.test.ts`

**Interfaces:**
- Consumes: `parseRange`/`rangeWhere` (T3), `Kpi` (T2), `seedAppeals` фикстура (T3).
- Produces:
  - `repo/kpi.ts`: `getKpi(pool, r: RangeFilter): Promise<Kpi>`. Логика: `total` за период; `prevTotal` за предыдущий равный период (сдвиг `[from,to]` назад на его длину; если `from`/`to` не заданы — период = последние 30 дней от `max(created_at)`, предыдущий — 30 дней до него); `deltaPct = prevTotal ? (total-prevTotal)/prevTotal : 0`; `overdueShare = overdue/total`; `avgCloseHours` по закрытым; `repeatShare` = доля обращений, у которых по тому же `address` есть более раннее закрытое (`address IS NOT NULL`); `openNow = status IN ('new','routed','in_progress')` (без учёта периода — «сейчас»).
  - `routes/commandCenter.ts`: плагин; первый маршрут `GET /api/kpi`.

- [ ] **Step 1: Написать тест `apps/api/tests/kpi.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import pg from "pg";
import { makeTestServer } from "./_server";

let app: FastifyInstance;
let pool: pg.Pool;
beforeAll(async () => { ({ app, pool } = await makeTestServer()); });
afterAll(async () => { await app.close(); await pool.end(); });

test("GET /api/kpi returns shape with sane ranges", async () => {
  const res = await app.inject({ method: "GET", url: "/api/kpi" });
  expect(res.statusCode).toBe(200);
  const k = res.json();
  for (const key of ["total", "prevTotal", "deltaPct", "overdueShare", "repeatShare", "openNow"]) {
    expect(typeof k[key]).toBe("number");
  }
  expect(k.total).toBeGreaterThan(0);
  expect(k.overdueShare).toBeGreaterThanOrEqual(0);
  expect(k.overdueShare).toBeLessThanOrEqual(1);
});

test("GET /api/kpi?region=akmola narrows the count", async () => {
  const all = (await app.inject({ method: "GET", url: "/api/kpi" })).json();
  const one = (await app.inject({ method: "GET", url: "/api/kpi?region=akmola" })).json();
  expect(one.total).toBeLessThan(all.total);
  expect(one.total).toBeGreaterThan(0);
});

test("GET /api/kpi?region=narnia -> 400 envelope", async () => {
  const res = await app.inject({ method: "GET", url: "/api/kpi?from=nope" });
  expect(res.statusCode).toBe(400);
  expect(res.json().error.code).toBe("bad_request");
});
```

- [ ] **Step 2: Запустить — падает** (`npm test -w @zerde/api -- kpi` → 404/нет маршрута).

- [ ] **Step 3: Написать `apps/api/src/repo/kpi.ts`**

```ts
import type pg from "pg";
import type { Kpi, RangeFilter } from "@zerde/types";

// kpi считает период и предыдущий-период вручную (rangeWhere здесь не подходит —
// нужна арифметика по датам), поэтому rangeWhere не импортируем.
export async function getKpi(pool: pg.Pool, r: RangeFilter): Promise<Kpi> {
  // границы периода
  const bounds = await pool.query<{ lo: string; hi: string }>(
    r.from && r.to
      ? `SELECT $1::timestamptz AS lo, ($2::date + interval '1 day') AS hi`
      : `SELECT (max(created_at) - interval '30 days') AS lo, max(created_at) AS hi FROM appeals`,
    r.from && r.to ? [`${r.from}T00:00:00+05:00`, r.to] : [],
  );
  const lo = bounds.rows[0]!.lo;
  const hi = bounds.rows[0]!.hi;
  const spanMs = new Date(hi).getTime() - new Date(lo).getTime();
  const prevLo = new Date(new Date(lo).getTime() - spanMs).toISOString();

  const extra: string[] = [];
  const extraP: unknown[] = [];
  let i = 3;
  if (r.region) { extra.push(`region = $${i++}`); extraP.push(r.region); }
  if (r.theme) { extra.push(`theme = $${i++}`); extraP.push(r.theme); }
  const extraClause = extra.length ? ` AND ${extra.join(" AND ")}` : "";

  const main = await pool.query<{
    total: string; overdue: string; avg_close: string | null;
  }>(
    `SELECT count(*) total,
            count(*) FILTER (WHERE is_overdue) overdue,
            avg(EXTRACT(EPOCH FROM (closed_at - created_at))/3600.0) FILTER (WHERE closed_at IS NOT NULL) avg_close
       FROM appeals WHERE created_at >= $1 AND created_at < $2${extraClause}`,
    [lo, hi, ...extraP],
  );
  const prev = await pool.query<{ total: string }>(
    `SELECT count(*) total FROM appeals WHERE created_at >= $1 AND created_at < $2${extraClause}`,
    [prevLo, lo, ...extraP],
  );
  const repeats = await pool.query<{ n: string }>(
    `SELECT count(*) n FROM appeals a
      WHERE a.address IS NOT NULL AND a.created_at >= $1 AND a.created_at < $2${extraClause}
        AND EXISTS (SELECT 1 FROM appeals b
                     WHERE b.address = a.address AND b.region = a.region
                       AND b.created_at < a.created_at)`,
    [lo, hi, ...extraP],
  );
  const openParts: string[] = [];
  const openP: unknown[] = [];
  if (r.region) { openParts.push(`region = $${openP.length + 1}`); openP.push(r.region); }
  if (r.theme) { openParts.push(`theme = $${openP.length + 1}`); openP.push(r.theme); }
  const open = await pool.query<{ n: string }>(
    `SELECT count(*) n FROM appeals WHERE status IN ('new','routed','in_progress')${
      openParts.length ? ` AND ${openParts.join(" AND ")}` : ""
    }`,
    openP,
  );

  const total = Number(main.rows[0]!.total);
  const prevTotal = Number(prev.rows[0]!.total);
  return {
    total,
    prevTotal,
    deltaPct: prevTotal ? (total - prevTotal) / prevTotal : 0,
    overdueShare: total ? Number(main.rows[0]!.overdue) / total : 0,
    avgCloseHours: main.rows[0]!.avg_close ? Math.round(Number(main.rows[0]!.avg_close)) : null,
    repeatShare: total ? Number(repeats.rows[0]!.n) / total : 0,
    openNow: Number(open.rows[0]!.n),
  };
}
```

- [ ] **Step 4: `apps/api/src/routes/commandCenter.ts`**

```ts
import type { FastifyInstance } from "fastify";
import { getPool } from "../db";
import { parseRange } from "../schema";
import { getKpi } from "../repo/kpi";

export async function commandCenterRoutes(app: FastifyInstance): Promise<void> {
  app.get("/api/kpi", async (req) => getKpi(getPool(), parseRange(req.query)));
}
```

- [ ] **Step 5: Register в `server.ts`**, затем `npm run check && npm test -w @zerde/api -- kpi` → PASS (3).

- [ ] **Step 6: Commit** `feat(api): GET /api/kpi with previous-period delta`.

---

## Task 5: `GET /api/timeseries`

**Files:** Create `apps/api/src/repo/timeseries.ts`; Modify `routes/commandCenter.ts`; Test `apps/api/tests/timeseries.test.ts`.

**Interfaces:**
- Produces: `repo/timeseries.ts` → `getTimeseries(pool, r: RangeFilter, granularity: Granularity): Promise<TimeseriesResponse>`. `date_trunc(granularity, created_at)` в поясе `Asia/Almaty`; заполняем пропуски нулями через `generate_series`. `points[]` отсортированы по `bucket` (ISO-дата начала корзины).

- [ ] **Step 1: Тест** — `GET /api/timeseries?granularity=month` → `points` непустой, каждый `{bucket, count, overdue}`; сумма `count` == kpi.total за тот же период (при отсутствии фильтра дат сверять с `SELECT count(*) FROM appeals`); неверный `granularity` → 400.
- [ ] **Step 2: Запустить — падает.**
- [ ] **Step 3: Реализация**

```ts
import type pg from "pg";
import type { Granularity, RangeFilter, TimeseriesResponse } from "@zerde/types";
import { rangeWhere } from "../schema";

export async function getTimeseries(
  pool: pg.Pool, r: RangeFilter, granularity: Granularity,
): Promise<TimeseriesResponse> {
  const { clause, params } = rangeWhere(r);
  const rows = await pool.query<{ bucket: string; count: string; overdue: string }>(
    `WITH b AS (
       SELECT date_trunc($${params.length + 1}, created_at AT TIME ZONE 'Asia/Almaty') AS bucket,
              count(*) AS count,
              count(*) FILTER (WHERE is_overdue) AS overdue
         FROM appeals ${clause}
        GROUP BY 1)
     SELECT to_char(bucket, 'YYYY-MM-DD') bucket, count, overdue FROM b ORDER BY bucket`,
    [...params, granularity],
  );
  return {
    granularity,
    points: rows.rows.map((x) => ({
      bucket: x.bucket, count: Number(x.count), overdue: Number(x.overdue),
    })),
  };
}
```

Маршрут: `zGranularity = z.enum(["day","week","month"]).default("month")`; `GET /api/timeseries`.

- [ ] **Step 4: Запустить — PASS** (`npm test -w @zerde/api -- timeseries`).
- [ ] **Step 5: Commit** `feat(api): GET /api/timeseries (day/week/month)`.

---

## Task 6: `GET /api/breakdown`

**Files:** Create `apps/api/src/repo/breakdown.ts`; Modify `routes/commandCenter.ts`, `server.ts` (уже подключён `commandCenterRoutes`); Test `apps/api/tests/breakdown.test.ts`.

**Interfaces:**
- Consumes: `rangeWhere`/`parseRange` (T3), `BreakdownDim`/`BreakdownRow` (T2), `getMeta` (T3, для подписей).
- Produces: `getBreakdown(pool, r: RangeFilter, dim: BreakdownDim): Promise<BreakdownRow[]>`. Колонка берётся из ФИКСИРОВАННОГО маппинга `{ region:"region", theme:"theme", status:"status", channel:"channel" }[dim]` (не из query). `label`: для `theme`/`region` — `name_ru` из справочника; для `status`/`channel` — из словарей `repo/meta.ts`. `deltaPct` — доля прироста ключа против предыдущего равного периода. Сортировка `count desc`.

- [ ] **Step 1: Тест `apps/api/tests/breakdown.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import pg from "pg";
import { makeTestServer } from "./_server";

let app: FastifyInstance; let pool: pg.Pool;
beforeAll(async () => { ({ app, pool } = await makeTestServer()); });
afterAll(async () => { await app.close(); await pool.end(); });

test("GET /api/breakdown?dim=theme — rows sum to total, labels present", async () => {
  const total = (await app.inject({ url: "/api/kpi" })).json().total;
  const res = await app.inject({ url: "/api/breakdown?dim=theme" });
  expect(res.statusCode).toBe(200);
  const rows = res.json() as { key: string; label: string; count: number }[];
  expect(rows.reduce((s, r) => s + r.count, 0)).toBe(total);
  expect(rows.every((r) => r.label.length > 0)).toBe(true);
});

test("GET /api/breakdown?dim=hacky -> 400", async () => {
  const res = await app.inject({ url: "/api/breakdown?dim=hacky" });
  expect(res.statusCode).toBe(400);
});
```

- [ ] **Step 2: Запустить — падает.**
- [ ] **Step 3: Реализация `repo/breakdown.ts`**

```ts
import type pg from "pg";
import type { BreakdownDim, BreakdownRow, RangeFilter } from "@zerde/types";
import { rangeWhere } from "../schema";
import { getMeta } from "./meta";

const COL: Record<BreakdownDim, string> = {
  region: "region", theme: "theme", status: "status", channel: "channel",
};

export async function getBreakdown(
  pool: pg.Pool, r: RangeFilter, dim: BreakdownDim,
): Promise<BreakdownRow[]> {
  const col = COL[dim];
  const { clause, params } = rangeWhere(r);
  const cur = await pool.query<{ key: string | null; count: string; overdue: string }>(
    `SELECT ${col} AS key, count(*) count, count(*) FILTER (WHERE is_overdue) overdue
       FROM appeals ${clause} GROUP BY ${col} ORDER BY count DESC`,
    params,
  );
  const meta = await getMeta(pool);
  const labelOf = (key: string | null): string => {
    if (key === null) return "—";
    if (dim === "theme") return meta.themes.find((t) => t.code === key)?.nameRu ?? key;
    if (dim === "region") return meta.regions.find((x) => x.code === key)?.nameRu ?? key;
    if (dim === "status") return meta.statuses.find((x) => x.code === key)?.labelRu ?? key;
    return meta.channels.find((x) => x.code === key)?.labelRu ?? key;
  };
  return cur.rows.map((x) => ({
    key: x.key ?? "—", label: labelOf(x.key),
    count: Number(x.count), overdue: Number(x.overdue), deltaPct: 0,
  }));
}
```

(Расчёт `deltaPct` против предыдущего периода — по образцу `getKpi`; в v1 допустимо оставить `0`, если тест не требует иного. Если нужно — добавить второй запрос с `rangeWhere` на сдвинутый диапазон и посчитать по ключам.)

Маршрут в `commandCenter.ts`: `zDim = z.enum(["region","theme","status","channel"])`; `GET /api/breakdown` → `getBreakdown(getPool(), parseRange(req.query), zDim.parse((req.query as {dim?: string}).dim))`. Невалидный `dim` → zod бросит → 400.

- [ ] **Step 4: Запустить — PASS. Step 5: Commit** `feat(api): GET /api/breakdown by region|theme|status|channel`.

---

## Task 7: `GET /api/appeals` + `GET /api/appeals/:id`

**Files:** Create `apps/api/src/repo/appeals.ts`, `apps/api/src/routes/intake.ts`; Modify `server.ts` (register `intakeRoutes`); Test `apps/api/tests/appeals.test.ts`.

**Interfaces:**
- Consumes: `zRange`/`zPagination`/`rangeWhere` (T3), `Paginated`/`AppealListItem`/`Appeal` (T2), `notFound` (T1).
- Produces:
  - `repo/appeals.ts`:
    - `type ListOpts = RangeFilter & { status?: AppealStatus; priority?: Priority; search?: string; page: number; pageSize: number; sort: "created_desc" | "created_asc" }`
    - `listAppeals(pool, opts: ListOpts): Promise<Paginated<AppealListItem>>` — `WHERE` из `rangeWhere` + опц. `status`/`priority` + `search` (`search_text ILIKE '%'||$n||'%'`); `preview = left(coalesce(raw_category,'')||' — '||coalesce(subcategory,''), 140)`; `ORDER BY created_at ${sort==='created_asc'?'ASC':'DESC'}`; `LIMIT pageSize OFFSET (page-1)*pageSize`. Отдельный `count(*)` с тем же `WHERE` → `total`.
    - `getAppeal(pool, id): Promise<Appeal | null>` — `SELECT * ... WHERE id=$1`; маппинг snake→camel, даты через `.toISOString()`.
  - `routes/intake.ts`: `GET /api/appeals` (query: `zRange.merge(zPagination).extend({ status: zStatus.optional(), priority: zPriority.optional(), search: z.string().optional(), sort: z.enum(["created_desc","created_asc"]).default("created_desc") })`); `GET /api/appeals/:id` → `getAppeal` или `throw notFound(...)`.

- [ ] **Step 1: Тест `apps/api/tests/appeals.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import pg from "pg";
import { makeTestServer } from "./_server";

let app: FastifyInstance; let pool: pg.Pool;
beforeAll(async () => { ({ app, pool } = await makeTestServer()); });
afterAll(async () => { await app.close(); await pool.end(); });

test("list: default page, total matches fixture count", async () => {
  const dbTotal = Number((await pool.query("SELECT count(*) c FROM appeals")).rows[0].c);
  const res = await app.inject({ url: "/api/appeals" });
  expect(res.statusCode).toBe(200);
  const body = res.json();
  expect(body.total).toBe(dbTotal);
  expect(body.items.length).toBeLessThanOrEqual(50);
  expect(body.items[0]).toHaveProperty("preview");
});

test("filters: region + status", async () => {
  const res = await app.inject({ url: "/api/appeals?region=akmola&status=done" });
  const body = res.json();
  expect(body.items.every((i: { region: string; status: string }) => i.region === "akmola" && i.status === "done")).toBe(true);
});

test("search finds the repeat-address rows", async () => {
  const res = await app.inject({ url: "/api/appeals?search=" + encodeURIComponent("Повторная") });
  expect(res.json().total).toBeGreaterThan(0);
});

test("pagination slices", async () => {
  const p1 = (await app.inject({ url: "/api/appeals?page=1&pageSize=5" })).json();
  const p2 = (await app.inject({ url: "/api/appeals?page=2&pageSize=5" })).json();
  expect(p1.items).toHaveLength(5);
  expect(p1.items[0].id).not.toBe(p2.items[0]?.id);
});

test("GET /api/appeals/:id and 404", async () => {
  const ok = await app.inject({ url: "/api/appeals/akmola:t1" });
  expect(ok.statusCode).toBe(200);
  expect(ok.json().id).toBe("akmola:t1");
  const no = await app.inject({ url: "/api/appeals/nope:0" });
  expect(no.statusCode).toBe(404);
  expect(no.json().error.code).toBe("not_found");
});
```

- [ ] **Step 2: Запустить — падает.**
- [ ] **Step 3: Реализация** `repo/appeals.ts` + `routes/intake.ts` по сигнатурам выше. `zStatus = z.enum(["new","routed","in_progress","done","cancelled"])`, `zPriority = z.enum(["low","medium","high"])` — вынести в `schema.ts`.
- [ ] **Step 4: Запустить — PASS. Step 5: Commit** `feat(api): GET /api/appeals (paginated/filtered) + /api/appeals/:id`.

---

## Task 8: `GET /api/spikes` + `GET /api/forecast`

**Files:** Create `apps/api/src/repo/spikes.ts`, `apps/api/src/repo/forecast.ts`; Modify `routes/commandCenter.ts`; Test `apps/api/tests/spikes-forecast.test.ts`.

**Interfaces:**
- Consumes: `SpikeRow`/`ForecastResponse`/`ForecastRow` (T2), `parseRange` (T3). `_server.ts` уже вызывает `computeSpikes`/`computeForecasts` (T3 Step 5).
- Produces:
  - `getSpikes(pool, r: RangeFilter): Promise<SpikeRow[]>` — `SELECT ... FROM spikes` + опц. `region`/`theme` + опц. `day BETWEEN from AND to`; `ORDER BY zscore DESC LIMIT 100`.
  - `getForecast(pool, region: string, theme: string): Promise<ForecastResponse>` — `history`: `SELECT to_char(date_trunc('month', created_at),'YYYY-MM-DD') month, count(*) FROM appeals WHERE region=$1 AND theme=$2 GROUP BY 1 ORDER BY 1` (последние 24); `forecast`: `SELECT ... FROM forecasts WHERE region=$1 AND theme=$2 ORDER BY month`; `method` = первая строка или `"seasonal-naive"`.
- Маршруты: `GET /api/spikes` (query `zRange`); `GET /api/forecast` (query `z.object({ region: z.string(), theme: z.enum(THEME_CODES...) })` — оба обязательны, иначе zod → 400).

- [ ] **Step 1: Тест `apps/api/tests/spikes-forecast.test.ts`**

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import type { FastifyInstance } from "fastify";
import pg from "pg";
import { makeTestServer } from "./_server";

let app: FastifyInstance; let pool: pg.Pool;
beforeAll(async () => { ({ app, pool } = await makeTestServer()); });
afterAll(async () => { await app.close(); await pool.end(); });

test("GET /api/spikes -> 200 array (may be empty on small fixture)", async () => {
  const res = await app.inject({ url: "/api/spikes" });
  expect(res.statusCode).toBe(200);
  expect(Array.isArray(res.json())).toBe(true);
});

test("GET /api/forecast?region=akmola&theme=water -> {history, forecast}", async () => {
  const res = await app.inject({ url: "/api/forecast?region=akmola&theme=water" });
  expect(res.statusCode).toBe(200);
  const b = res.json();
  expect(Array.isArray(b.history)).toBe(true);
  expect(Array.isArray(b.forecast)).toBe(true);
  expect(typeof b.method).toBe("string");
});

test("GET /api/forecast without params -> 400", async () => {
  const res = await app.inject({ url: "/api/forecast?region=akmola" });
  expect(res.statusCode).toBe(400);
});
```

- [ ] **Step 2: Запустить — падает. Step 3: Реализация по сигнатурам. Step 4: PASS. Step 5: Commit** `feat(api): GET /api/spikes + /api/forecast`.

---

## Task 9: `POST /api/classify` + `classifyText`

**Files:**
- Create: `packages/ingest/src/classifyText.ts`
- Modify: `packages/ingest/src/lib.ts` (раскомментировать экспорт)
- Create: `apps/api/src/repo/classify.ts` (тонкая обёртка), route в `routes/intake.ts`
- Test: `packages/ingest/tests/classifyText.test.ts`, `apps/api/tests/classify.test.ts`

**Interfaces:**
- Produces:
  - `classifyText(text: string, ctx: { rules: ThemeRule[]; themeService: Record<ThemeCode,string> }): ClassifyTextResult` где `ClassifyTextResult = ClassifyResult` (из `@zerde/types`). Логика:
    - `language = detectLanguage(text)`
    - тема: скоринг — для каждого правила, чей `pattern` входит в `text.toLowerCase()`, прибавить `weight = 1/priority` к его теме; тема с макс. суммой; если сумма 0 → `other`.
    - `confidence` = `top / (top + second)` (0.5..1), при 0 совпадений → 0.3.
    - `service = themeService[theme]`
    - `priority = priorityHeuristic({ theme, appealType: /авари|срочно|прорыв|порыв|нет .* (воды|света|тепла|газа)/i.test(text) ? "incident" : null, isOverdue: false })`
    - `entities.address` — регуляркой: `/(ул\.|улица|мкр|дом|переулок|проспект|пр\.|көше|№)\s*[^,.;\n]{2,40}/i` → первый матч, трим.
    - `entities.problem` — первое предложение до 80 символов.
    - `entities.object` — из ключевых слов (`/(колодец|фонарь|светофор|труба|контейнер|остановка|дорога|подъезд|лифт)/i`) первый матч или null.
  - `apps/api`: `POST /api/classify` body `{ text: string (1..4000), language?: "kk"|"ru" }` → `ClassifyResult`. Правила и `THEME_SERVICE` берём из `@zerde/ingest` (`THEME_RULES`, `THEME_SERVICE`).

- [ ] **Step 1**: раскомментировать `classifyText` в `lib.ts`.
- [ ] **Step 2**: тест `classifyText.test.ts` — таблица кейсов:
  - `"Көшеде жарық жоқ, үшінші күн"` → `theme:"lighting"`, `language:"kk"`, `confidence>0.5`
  - `"Прорыв трубы, нет воды в доме по ул. Абая 12"` → `theme:"water"`, `priority:"high"`, `entities.address` содержит `"Абая"`
  - `"Не работает светофор на перекрёстке"` → `theme:"roads"`, `entities.object:"светофор"`
  - `"к136 zzz непонятно"` → `theme:"other"`, `confidence` ≈ 0.3
- [ ] **Step 3**: реализация (падает → пишем → проходит).
- [ ] **Step 4**: `apps/api/tests/classify.test.ts` — `POST /api/classify {text:"нет отопления, батареи холодные"}` → 200, `theme:"heating"`; `{text:""}` → 400.
- [ ] **Step 5**: Commit `feat(api): POST /api/classify + reusable classifyText`.

---

## Task 10: `model_eval` — таблицы, бэктест, `GET /api/model-eval`

**Files:**
- Create: `db/migrations/0004_model_eval.sql`
- Create: `packages/ingest/src/analytics/modelEval.ts`, `packages/ingest/src/eval.ts`
- Modify: `packages/ingest/package.json` (`"eval": "tsx src/eval.ts"`), корневой `package.json` (`"eval": "npm run -s eval -w @zerde/ingest"`)
- Create: `apps/api/src/repo/modelEval.ts`, route в `routes/intake.ts`
- Test: `packages/ingest/tests/modelEval.test.ts`, `apps/api/tests/model-eval.test.ts`

**Interfaces:**
- `0004_model_eval.sql`:
  ```sql
  CREATE TABLE IF NOT EXISTS model_eval_overview (
    id bool PRIMARY KEY DEFAULT true CHECK (id),
    method text NOT NULL, computed_at timestamptz NOT NULL DEFAULT now(),
    n_holdout int NOT NULL, accuracy double precision NOT NULL, macro_f1 double precision NOT NULL);
  CREATE TABLE IF NOT EXISTS model_eval_themes (
    theme text PRIMARY KEY, precision double precision NOT NULL, recall double precision NOT NULL,
    f1 double precision NOT NULL, support int NOT NULL);
  CREATE TABLE IF NOT EXISTS model_eval_confusion (
    actual text NOT NULL, predicted text NOT NULL, n int NOT NULL, PRIMARY KEY (actual, predicted));
  ```
- `analytics/modelEval.ts`: `computeModelEval(pool): Promise<{ nHoldout: number; accuracy: number; macroF1: number }>`. Бэктест: выборка `appeals` где `raw_category IS NOT NULL`, holdout = `abs(hashtext(id)) % 5 = 0` (~20%, детерминированно). «Текст» = `raw_category || ' ' || coalesce(subcategory,'')`. `predicted = classifyText(text, ctx).theme`. `actual = appeals.theme`. Считаем per-theme precision/recall/f1/support, overall accuracy, macro-F1, матрицу ошибок. `TRUNCATE` три таблицы, вставляем. **Метод в overview:** `"keyword-baseline (proxy text = raw_category+subcategory)"` — честно про прокси.
- `eval.ts`: `isMain` → migrate + `computeModelEval(getPool())` + печать сводки.
- `repo/modelEval.ts`: `getModelEval(pool): Promise<ModelEval | null>` — собрать из трёх таблиц; `null` если `model_eval_overview` пуст.
- `GET /api/model-eval` → `ModelEval`; если null → 404 `notFound("model eval not computed — run npm run eval")`.

- [ ] Тест `modelEval.test.ts`: на тестовой БД (фикстура `seedAppeals` даёт `raw_category='тест-категория'` для всех — бэктест сойдётся тривиально, но проверяем ФОРМУ: `computeModelEval` возвращает `{nHoldout>0, accuracy 0..1, macroF1 0..1}` и заполняет 3 таблицы). Для осмысленных чисел — отдельная фикстура: вставить 200 строк с `raw_category` из реального словаря (`'Уличное освещение'`, `'Отсутствие воды'`, …) и `theme` проставленный правильно; проверить `accuracy > 0.6`.
- [ ] Тест `model-eval.test.ts`: до `computeModelEval` → `GET /api/model-eval` = 404; после — 200 с `perTheme[]`, `confusion[]`, `0 ≤ accuracy ≤ 1`.
- [ ] Commit `feat(api): model-eval backtest tables + GET /api/model-eval`.

---

## Task 11: `POST /api/nl-query` (5 интентов)

**Files:**
- Create: `apps/api/src/nl/intents.ts`, `apps/api/src/nl/sqlBuilder.ts`, `apps/api/src/repo/nlQuery.ts`
- Modify: `routes/commandCenter.ts`
- Test: `apps/api/tests/nl-query.test.ts`

**Interfaces:**
- `nl/intents.ts`: `parseIntent(q: string): ParsedIntent | null` где
  ```ts
  type ParsedIntent =
    | { kind: "count_by_theme_region_period"; theme: ThemeCode; region?: string; days: number }
    | { kind: "top_themes"; region?: string; days: number; n: number }
    | { kind: "theme_dynamics"; theme: ThemeCode; region?: string; days: number }
    | { kind: "overdue_share_by_region"; days: number }
    | { kind: "compare_regions"; regionA: string; regionB: string; theme?: ThemeCode; days: number };
  ```
  Парсинг: нормализовать строку в нижний регистр; распознать регион по списку синонимов (`{ akmola: ["акмол","кокшетау"], almaty:["алмат"], "east-kazakhstan":["восточно","вко","усть-камен"], karaganda:["караганд"], kostanay:["костанай"], turkestan:["туркестан","шымкент"], pavlodar:["павлодар"] }`); тему — по подстрокам из `THEME_RULES` названий/ключей (`{water:["вод"],roads:["дорог","дорож"],lighting:["освещен","свет"],heating:["отоплен","тепло"],...}`); период — `за (последн\w+ )?(месяц|неделю|N дн\w*|год)` → `days` (30/7/N/365; дефолт 30); `top N` → `n` (дефолт 5). Порядок проверки интентов фиксирован; первый подошедший — победитель.
- `nl/sqlBuilder.ts`: `buildSql(intent: ParsedIntent): { sql: string; params: unknown[]; chart: NlQueryResult["chart"]; shape: "value" | "rows" }` — по одному фиксированному шаблону на `kind`, только параметризованные значения, имена колонок/периоды из кода.
- `repo/nlQuery.ts`: `runNlQuery(pool, q: string): Promise<NlQueryResult>` — `parseIntent` → `null` кидает `unrecognized("Не понял запрос. Примеры: …")`; иначе `buildSql`, выполнить, собрать `{ intent: kind, sql, value|rows, chart, summary }`. `summary` — короткая RU-строка с числом/выводом.
- `POST /api/nl-query` body `{ q: string (1..500) }`.

- [ ] Тест: для каждого из 5 примеров из спеки §5 — 200 + непустой результат нужной формы; `"свари борщ"` → 422 `code:"unrecognized_query"`; проверить, что `sql` в ответе не содержит подстроки из `q` (иньекции-защита — билдер игнорирует сырой текст).
- [ ] Commit `feat(api): POST /api/nl-query with 5 intent templates + safe SQL builder`.

---

## Task 12: Ассистент оператора — `similar`, `duplicates`, `templates`, `route`

**Files:**
- Create: `db/seeds/05_templates.sql`
- Create: `apps/api/src/repo/similar.ts`, `apps/api/src/repo/templates.ts`, `apps/api/src/repo/mutations.ts`
- Create: `apps/api/src/routes/operator.ts`; Modify `server.ts`
- Test: `apps/api/tests/operator.test.ts`

**Interfaces:**
- `05_templates.sql`: `DELETE FROM templates; INSERT …` — ~16 строк: по 1 RU + 1 KK на топ-темы (`water, electricity, heating, sewer, roads, lighting, waste, transport`), `service_code` из `THEME_SERVICE`, `title`/`body` с плейсхолдерами `{address}`, `{service}`.
- `repo/similar.ts`:
  - `getSimilar(pool, id: string, k = 8): Promise<SimilarAppeal[]>` — берём `search_text` целевого, ищем `WHERE id <> $1 AND theme = $targetTheme AND region = $targetRegion ORDER BY similarity(search_text, $target) DESC LIMIT k` (pg_trgm `similarity`), `daysToClose` = `EXTRACT(day FROM closed_at - created_at)`.
  - `getDuplicates(pool, id): Promise<DuplicateInfo>` — `nearDuplicates`: `similarity > 0.6` и `status` открытый; `repeats`: тот же `address` (не null) и `region`, `closed_at > now() - interval '30 days'` (в тестах — от `max(created_at)` фикстуры; использовать `GREATEST(now(), (SELECT max(created_at) FROM appeals))` как «сейчас»). 404 если `id` нет.
- `repo/templates.ts`: `getTemplates(pool, opts: { theme?: ThemeCode; service?: string; lang?: Language }): Promise<Template[]>`.
- `repo/mutations.ts`: `recordMutation(pool, appealId: string, kind: "route"|"draft"|"mark_duplicate", payload: object): Promise<{ id: number }>` — `INSERT INTO mutations`; если `kind="route"` и `payload.status` валиден — доп. `UPDATE appeals SET status=$ WHERE id=$` (единственное место, где API пишет в `appeals`; допустимо — это действие оператора, не исходные данные). 404 если обращения нет.
- Маршруты: `GET /api/appeals/:id/similar`, `GET /api/appeals/:id/duplicates`, `GET /api/templates`, `POST /api/appeals/:id/route` body `{ kind, payload }` (zod: `kind` enum, `payload` `z.record(z.unknown())`).

- [ ] Тест: `similar` для `akmola:t1` → массив (может быть коротким), каждый `similarity` в 0..1, `id` != запрошенного; `duplicates` для повторного адреса → `repeats` непустой; `templates?theme=water` → ≥1; `POST /api/appeals/akmola:t1/route {kind:"route",payload:{status:"in_progress"}}` → 200 `{id}`, и `GET /api/appeals/akmola:t1` теперь `status:"in_progress"`, и строка в `mutations`; тот же POST на несуществующий id → 404.
- [ ] Commit `feat(api): operator assistant endpoints (similar/duplicates/templates/route) + templates seed`.

---

## Task 13: `POST /api/report` (xlsx + pdf)

**Files:** Create `apps/api/src/repo/report.ts`, `apps/api/src/routes/report.ts`; Modify `server.ts`; Test `apps/api/tests/report.test.ts`.

**Interfaces:**
- `repo/report.ts`:
  - `buildReportData(pool, view: "overview"|"regions"|"themes", r: RangeFilter): Promise<{ columns: string[]; rows: (string|number)[][]; title: string }>` — переиспользует `getBreakdown`/`getKpi`/`getTimeseries`.
  - `toXlsx(data): Promise<Buffer>` — `exceljs`, один лист, заголовок жирный.
  - `toPdf(data): Promise<Buffer>` — `pdfkit`, заголовок + простая таблица (моноширинный, фикс. ширины колонок).
- `POST /api/report` body `{ view, format: "xlsx"|"pdf", filters?: RangeFilter }` → бинарь с корректными `Content-Type` (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` / `application/pdf`) и `Content-Disposition: attachment; filename="zerde-<view>-<date>.<ext>"`.

- [ ] Тест: `POST /api/report {view:"regions",format:"xlsx"}` → 200, `content-type` xlsx, тело длиной > 1000 байт, начинается с `PK` (zip-сигнатура); `format:"pdf"` → тело начинается с `%PDF`; `format:"csv"` → 400.
- [ ] Commit `feat(api): POST /api/report (xlsx + pdf export)`.

---

## Task 14: Сборка, смоук на реальной БД, финал

**Files:** Modify `apps/api/src/server.ts` (все роут-плагины подключены); Create `apps/api/src/smoke.ts`; Modify корневой `package.json` (`"smoke:api"`).

**Interfaces:**
- `apps/api/src/smoke.ts`: `isMain` → `buildServer`, `inject` по списку эндпоинтов против **рабочей** `zerde109`, печать `METHOD URL -> status (ms)`; выход 1, если любой не 2xx (кроме `/api/model-eval`, который допускает 404 до `npm run eval`).

- [ ] **Step 1:** Убедиться, что `server.ts` регистрирует `metaRoutes`, `commandCenterRoutes`, `intakeRoutes`, `operatorRoutes`, `reportRoutes`.
- [ ] **Step 2:** `npm run check` → 0. `npm test` (все воркспейсы) → зелено.
- [ ] **Step 3:** `npm run db:migrate` (накатит `0004`). Опц.: `npm run eval` (заполнит `model_eval*` из рабочей БД — несколько минут).
- [ ] **Step 4:** `npm run smoke:api` → все строки 2xx (или 404 для model-eval, если `eval` не запускали).
- [ ] **Step 5:** `npm run dev:api` вручную, `curl http://127.0.0.1:3001/api/meta` — приходит JSON. Остановить.
- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(api): wire all route plugins + real-DB smoke script

Витрина -> Fastify API готов: meta, kpi, timeseries, breakdown, appeals,
spikes, forecast, classify, model-eval, nl-query, operator, report.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>"
```

---

## Definition of Done (План 2)

- [ ] `npm run check` (tsc ×3) — без ошибок.
- [ ] `npm test` — `@zerde/types` (нет тестов), `@zerde/ingest` (139+ зелёных), `@zerde/api` (все зелёные).
- [ ] `npm run smoke:api` против `zerde109` — все эндпоинты 2xx (model-eval — 2xx после `npm run eval`, иначе 404).
- [ ] `apps/api` запускается `npm run dev:api`, `/api/meta` отдаёт реальные справочники, `/api/kpi` — реальные числа по ~990k обращений.
- [ ] Каждая задача — отдельный коммит.

## Self-review (план против спеки §5)

- **§5 «Ситуационный центр»:** `/kpi` T4, `/timeseries` T5, `/breakdown` T6, `/spikes`+`/forecast` T8, `/nl-query` T11, `/report` T13. ✓
- **§5 «Смарт-приём»:** `/appeals`+`/appeals/:id` T7, `/classify` T9, `/model-eval` T10. ✓
- **§5 «Ассистент оператора»:** `/similar`, `/duplicates`, `/templates`, `/route` T12. ✓
- **§5 «Общее»:** `/meta` T3. ✓
- **§5 «Заглушки моделей»:** `/classify` — детект языка + взвешенные правила + приоры + сущности (T9); `/nl-query` — интент-паттерны + фиксированный SQL-билдер, сырой текст в SQL не попадает (T11, тест на это). ✓
- **§5 валидация/ошибки:** zod preHandler-парс в каждом маршруте, конверт `{error:{code,message}}`, коды 400/404/422/500 (T1 `errors.ts`, проверяется в тестах T4/T7/T11). ✓
- **§4.3 `model_eval`:** перенесён сюда (T10), реализован через таблицы `model_eval*`, не JSON-файл (чище для чтения из API); метод честно помечен как proxy-baseline. Отклонение от формулировки спеки — задокументировано.
- **§4.2 `templates`:** засеяны в T12 (`05_templates.sql`).
- **Плейсхолдеры:** блоки задач T5/T6/T7/T8 намеренно ужаты (тест-суть + сигнатура + ключевой код), т.к. паттерн «zod-парс → repo-функция → inject-тест» повторяется; каждый repo-модуль содержит полный SQL. T3/T4/T9/T10/T11 — с полным кодом.
- **Согласованность типов:** все ответы — интерфейсы из `@zerde/types` (T2), имена полей едины между `repo/*`, тестами и типами. `rangeWhere`/`parseRange` — единый источник разбора фильтров.
